<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.bible.bible_study_back.mapper.PBSMapper">
    <!-- 모든 데이터 -->
    <select id="findAll" resultType="com.bible.bible_study_back.dto.PBSDTO">
        select * from pbs order by id desc
    </select>
    <!-- 커뮤니티 페이지 ( 10개 ) -->
    <select id="findByPage" resultType="com.bible.bible_study_back.dto.PostDTO">
        select * from post order by id desc limit #{count} offset #{offset}
    </select>
    <!-- 상세 페이지 -->
    <select id="findOne" resultType="com.bible.bible_study_back.dto.PostDTO">
        select * from post where id = #{id}
    </select>
    <!-- PBS 생성 -->
    <insert id="save">
        insert into post(title, content) values (#{post.title}, #{post.content})
    </insert>


</mapper>