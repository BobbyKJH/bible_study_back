<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="com.bible.bible_study_back.mapper.AnalysisMapper">

    <!--PBS 날짜별 통계-->
    <select id="analysisPBS" resultType="com.bible.bible_study_back.dto.AnalysisDto">
        select date date, count(*) chart from pbs group by date;
    </select>

</mapper>